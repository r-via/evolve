"""evolve.domain — pure business concepts, no I/O, no SDK.

This package holds the ubiquitous-language types that SPEC.md defines.
Domain imports nothing from evolve (SPEC § DDD dependency rule).
"""
