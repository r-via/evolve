"""Use case: multi-agent party mode session.

Operator-interface bounded context — depends only on evolve.domain.
"""

from __future__ import annotations


def run_party_session(
    spec_path: str | None = None,
) -> None:
    """Run the post-convergence party mode brainstorm.

    Parameters
    ----------
    spec_path:
        Path to the spec file for proposal generation.

    Raises
    ------
    NotImplementedError
        Stub — wiring to infrastructure pending.
    """
    raise NotImplementedError(
        "run_party_session stub — DDD migration in progress"
    )
