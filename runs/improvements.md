# Improvements

- [x] [functional] Implement hooks.py module and integrate event hooks (on_round_start, on_round_end, on_converged, on_error) into the evolution loop as specified in the README
- [x] [functional] Add completion summary panel to TUI that displays improvements completed, bugs fixed, tests passing, and report path as described in README's "Completion summary" section
- [x] [functional] Implement real-time state.json file in session directory, updated after every round with version, session, project, round, max_rounds, phase, status, improvements counts, last_check results, and timestamps as described in README's "Real-time state file" section
- [x] [functional] Implement --validate flag for spec compliance checking: add CLI argument, create run_validate() function in loop.py, produce validate_report.md with pass/fail per README claim, return exit code 0 (all pass) or 1 (failures) as described in README's "--validate flag" section
- [x] [performance] Reduce code duplication in agent.py by extracting shared agent runner logic from _run_dry_run_claude_agent, _run_validate_claude_agent, and run_claude_agent into a reusable helper, since all three share nearly identical SDK streaming, deduplication, and logging code
- [x] [performance] Extract duplicated README/improvements loading logic from build_prompt, build_validate_prompt, and build_dry_run_prompt into a shared _load_project_context helper to reduce repetition and centralize file-loading logic
- [x] [performance] Extract duplicated check_section building logic from build_validate_prompt and build_dry_run_prompt into a shared _build_check_section helper, since both use the identical pattern for generating check command output sections
- [x] [functional] Add dedicated unit tests for the new _build_check_section helper in agent.py to verify all three branches (cmd+output, cmd-only, no cmd) and ensure the helper is tested independently from the prompt builders
- [x] [functional] Add auto-detection of test framework when --check is omitted: scan project for pytest, npm test, cargo test, go test, make test and use the first found, as described in the README's "--check flag" section
- [x] [functional] Add unit tests for _auto_detect_check integration in evolve_loop: verify that when check_cmd=None, evolve_loop calls _auto_detect_check and uses the detected command, and that an explicit --check flag bypasses auto-detection
- [x] [functional] Add tests for _generate_evolution_report to verify it produces correct timeline tables, summary stats, and handles edge cases (empty sessions, missing commit messages, partial round data)
- [x] [functional] Add tests for _parse_report_summary to verify it correctly extracts improvements completed, bugs fixed, files modified, and remaining counts from evolution_report.md content, including malformed and empty reports
- [x] [functional] Add tests for _update_state_json to verify it correctly writes and updates state.json with proper schema (version, session, project, round, phase, status, improvements counts, last_check, timestamps) and handles edge cases like missing directories and concurrent writes
- [x] [functional] Add tests for _setup_forever_branch and _forever_restart edge cases: verify branch naming with special characters, git failure recovery, README_proposal.md adoption with malformed content, and improvements.md reset behavior
- [x] [functional] Add tests for _ensure_git edge cases: verify auto-commit of uncommitted changes, behavior with uninitialized git repos, and handling of dirty working trees with mixed staged/unstaged changes
- [x] [functional] Add tests for hook environment variables: verify that EVOLVE_SESSION, EVOLVE_ROUND, and EVOLVE_STATUS are correctly set when hooks fire, and that hook commands can access them during execution
- [x] [functional] Add tests for TUI completion summary generation: verify that RichTUI, PlainTUI, and JsonTUI all produce correct completion summaries from evolution_report.md data, including edge cases like zero improvements, missing report files, and very long session durations
- [x] [functional] Add tests for _parse_improvements_md edge cases: verify correct parsing of malformed checkbox syntax, mixed indentation, empty lines between items, items with special characters, and ensure blocked/needs-package items are counted correctly
- [x] [functional] Add tests for _detect_last_round edge cases: verify correct detection with gaps in conversation logs, corrupted filenames, empty run directory, and sessions with only error logs but no conversation logs
- [x] [functional] Harden the resume sorting lambda on line 486 of loop.py to gracefully handle non-numeric conversation_loop filenames instead of crashing — currently `key=lambda p: int(p.stem.rsplit("_", 1)[1])` will raise ValueError on corrupted filenames like conversation_loop_abc.md
- [x] [functional] Add test coverage for _update_state_json's check_file parsing branch (loop.py lines 804-811) which extracts test count and duration from check_round_N.txt files — currently uncovered and parsing regex could silently fail on non-standard test output formats
- [x] [functional] Add party mode tests: verify _run_party_mode creates party_report.md and README_proposal.md, handles agent persona loading from agents/*.md, and gracefully handles missing workflow files
- [x] [functional] Add integration tests for the full evolve_loop with --forever flag: verify branch creation, convergence detection, party mode invocation, README_proposal adoption, improvements reset, and loop restart in a single end-to-end test
- [x] [functional] Add test coverage for evolve_loop with --resume and --forever combined: verify that resuming a forever-mode session correctly restores the branch, detects last round, and continues the evolution loop from the right starting point
- [x] [functional] Add test coverage for the uncovered lines in agent.py (lines 181-186, 390-393): verify the agent retry logic for rate limit errors with exponential backoff and the benign async teardown error detection paths
- [x] [functional] Add test coverage for the remaining uncovered lines in evolve.py (lines 62-63, 177-180, 185-190, 258-262, 269-270, 293): cover the main() entry point error paths, config file parsing edge cases, and CLI dispatch for history/clean subcommands with error conditions
- [x] [functional] Add test coverage for hooks.py uncovered lines 56-60 (hook execution timeout and subprocess error handling) to bring hooks.py from 91% to 100% coverage
- [x] [functional] Add test coverage for the remaining uncovered lines in loop.py (lines 71-72, 488, 522-523, 531-532, 729, 837-838, 1135, 1218) to bring loop.py from 98% to 100% coverage [blocked: target too broad — split required, rounds 3-5 of session 20260423_114700 made no progress on this item]
- [x] [functional] Implement the `--spec` CLI flag in evolve.py (and matching `spec` field in evolve.toml / `EVOLVE_SPEC` env var) so the spec file can be something other than README.md; the flag path must flow through to agent.py prompt builders and loop.py (dry-run, validate, party mode, forever restart) wherever README.md is currently hardcoded — see SPEC.md "The --spec flag" section
- [x] [functional] Remove the hardcoded `max_turns=20` from the two ClaudeAgentOptions invocations in agent.py (lines 216 and 587) so rounds are not killed mid-work on large targets; the 120s watchdog and round timeout are the only intended bounds on agent runtime — see SPEC.md "No per-turn cap" paragraph
- [x] [functional] Implement Phase 2 spec freshness gate in agent.py's system prompt and/or loop.py: before touching improvements.md, compare `mtime(README.md)` vs `mtime(improvements.md)`; if README is newer, mark every unchecked item as `[stale: spec changed]` and rebuild improvements.md from the current README (one item per unimplemented claim) — see SPEC.md Phase 2 and "Convergence" section
- [x] [functional] Implement zero-progress detection in loop.py's `_run_monitored_subprocess` / `_run_rounds`: count a round as no-progress (trigger debug retry with "CRITICAL — Previous round made NO PROGRESS" header) when the agent commits without writing COMMIT_MSG (orchestrator fell back to `chore(evolve): round N`) OR when improvements.md is byte-identical to its pre-round state — see SPEC.md "Subprocess monitoring & debug retries" section, "Zero progress detection" paragraph
- [x] [functional] Implement agent-side stuck-loop self-monitoring in agent.py's system prompt: instruct the agent to read `conversation_loop_{N-1}.md` and `conversation_loop_{N-2}.md` at round start, flag a stuck loop when the target matches and prior logs contain no Edit/Write calls, then either split the target into smaller items or mark it blocked and pick a different one — see SPEC.md "Agent-side self-monitoring" paragraph
- [x] [functional] Add tests for the --spec flag: verify that evolve.py exits with code 2 when the spec file doesn't exist, that _load_project_context reads the correct spec file, that _forever_restart derives the correct proposal filename (e.g. SPEC_proposal.md for --spec SPEC.md), and that _run_party_mode produces proposal files named after the spec
- [x] [functional] Raise the hardcoded `max_turns` in agent.py (lines 216 and 587) from `20` to `40` so rounds have enough headroom to finish the granular improvements without hitting the cap routinely, while preserving the cap as a granularity forcing function — see SPEC.md "Per-turn cap as a granularity forcing function" paragraph
- [x] [functional] Add tests for _check_spec_freshness: verify mtime comparison, stale marking of unchecked items, idempotent stale marking, preservation of checked items, handling of missing spec/improvements files, and convergence gate rejection when spec is newer than improvements.md
- [x] [functional] Add tests for zero-progress detection in _run_rounds: verify that improvements.md byte-identical pre/post round triggers no-progress retry, that missing COMMIT_MSG triggers no-progress retry, and that both conditions combined produce the correct reason string in the diagnostic
- [x] [functional] Rename `--yolo` to `--allow-installs` (and `yolo` to `allow_installs` in config, `EVOLVE_YOLO` env var to `EVOLVE_ALLOW_INSTALLS`) throughout evolve.py, loop.py, agent.py, hooks.py, pyproject.toml, tests, and prompts — keep `--yolo`/`yolo`/`EVOLVE_YOLO` as deprecated aliases that emit a `DeprecationWarning` to stderr and behave identically. See SPEC.md "--allow-installs mode" section for rationale and deprecation plan
- [x] [functional] Add tests for the `--allow-installs` rename: verify both the new flag and the deprecated `--yolo` alias produce the same behavior, that using `--yolo` emits a `DeprecationWarning`, and that the `evolve.toml` / env var renames are honored with fallback to the old names
- [x] [functional] Implement the Phase 1 escape hatch from SPEC.md § "Phase 1 escape hatch for unrelated pre-existing failures": at the start of the final retry attempt (attempt 3), the agent detects when Phase 1 errors are still present AND reference no files named in the current improvement target, then (a) logs the failing tests to memory.md under `## Blocked Errors` with full context, (b) appends a high-priority item to improvements.md of the form `[ ] [functional] Phase 1 bypass: fix pre-existing failures (<summary>) that blocked round N`, (c) proceeds with the original Phase 3 target while treating the pre-existing failures as known-broken state, (d) includes a top-level `Phase 1 bypass: <summary>` line in COMMIT_MSG so the bypass is visible in git history. Implement in agent.py's system prompt (to teach the agent the three conditions + the four actions) and in loop.py's retry prompt builder (to mark attempt 3 explicitly so the agent knows the bypass is now permitted). Only triggers on attempt 3 — never on attempts 1 or 2, which must still do normal Phase 1 work. See SPEC.md § "Phase 1 escape hatch" for the full predicate and guard conditions
- [x] [functional] Add tests for the Phase 1 escape hatch: verify the bypass does NOT trigger on attempt 1 or 2, verify it DOES trigger on attempt 3 when failing tests are in files not mentioned by the target, verify it does NOT trigger when failing tests reference files named in the target (must still Phase-1 them), verify the memory.md `## Blocked Errors` section is populated with the check output excerpt and bypass note, verify improvements.md gets the exact `Phase 1 bypass: ...` item appended, verify the commit message has the `Phase 1 bypass: <summary>` top-level line, verify the target's changes are committed alongside the bypass marker in a single commit
- [x] [functional] Implement Mechanism A "Pre-convergence README audit" from SPEC.md § "README sync discipline": before writing CONVERGED in loop.py, run a grep-level audit that extracts user-visible claims from the spec file (CLI flags from `^###? The --` headers, subcommands from `evolve <word>` patterns, env vars matching `EVOLVE_[A-Z_]+`, exhibited shell examples from `$` lines and ```bash fences, and `## Requirements` bullets) and for each claim not mentioned in README.md appends an improvements.md item of the form `[ ] [functional] README sync: mention <claim> in <README section> (documented in <spec> § <section> but absent from README.md)`. The audit must not block convergence — CONVERGED is still written; the sync items become the next cycle's first targets. Only runs when `--spec` points at a file other than README.md. See SPEC.md § "README sync discipline" Mechanism A
- [x] [functional] Implement Mechanism B "Party mode README proposal" from SPEC.md § "README sync discipline": extend `_run_party_mode` in loop.py to produce a third output `README_proposal.md` alongside `party_report.md` and `<spec>_proposal.md` when `--spec` points at a file other than README.md; agents must be instructed to preserve the README's tutorial voice (brevity, examples, links to SPEC.md for internals) rather than copying the spec verbatim. In `--forever` mode, `_forever_restart` must adopt both proposals atomically in a single commit (`SPEC.md ← SPEC_proposal.md` AND `README.md ← README_proposal.md`) with the documented commit message template. See SPEC.md § "README sync discipline" Mechanism B
- [x] [functional] Implement Mechanism C "README drift warning" from SPEC.md § "README sync discipline": track `readme_stale_since_round` in state.json, set it on every round where `mtime(spec_file) > mtime(README.md)`, clear it when README.md is touched; when `rounds_since_stale > 3`, emit a yellow `ui.warning` panel with the documented message "⚠️ README drift: SPEC.md touched N rounds ago, README.md unchanged" and populate `state.json.readme_sync` with `{stale, spec_mtime, readme_mtime, rounds_since_stale}`. Observability-only — never blocks convergence or triggers retry. Only active when `--spec` points at a file other than README.md. See SPEC.md § "README sync discipline" Mechanism C
- [x] [functional] Integration test for Mechanism C inside `_run_rounds`: using a stub `TUIProtocol` and a temp project, drive the round-loop body through 5+ simulated rounds where `mtime(SPEC.md) > mtime(README.md)` the whole time, and verify (a) `ui.warn` is first called at the exact round where `rounds_since_stale` crosses `3`, (b) the warning message matches the documented format "README drift: SPEC.md touched N rounds ago, README.md unchanged", (c) `state.json.readme_sync.rounds_since_stale` grows monotonically across rounds, (d) touching README.md mid-run resets the counter in the very next round's state.json, and (e) the warning/counter are never populated when `--spec` is unset or equals "README.md"
- [x] [functional] Add tests for the README sync discipline: verify Mechanism A extracts claims correctly across multiple spec formats and generates well-formed improvements items for each gap, verify A never blocks CONVERGED even when gaps exist, verify Mechanism B produces a valid README_proposal.md only when --spec differs from README.md, verify --forever adopts both proposals in a single commit with the documented message, verify Mechanism C's state.json fields and TUI warning fire at exactly the correct threshold (>3 rounds), and verify all three mechanisms are no-ops when --spec is unset or equals "README.md"
- [x] [functional] Implement retry continuity so debug retries reuse the previous attempt's work instead of re-investigating: (a) in loop.py write each attempt of round N to its own file `conversation_loop_N_attempt_{K}.md` (no overwrite; symlink or copy the final successful attempt to `conversation_loop_N.md` for backward compatibility with report generation, party mode, and self-monitoring), (b) extend the debug retry prompt builder in agent.py / loop.py to include a `## Previous attempt log` section with the full path to the prior `conversation_loop_N_attempt_{K-1}.md` and an explicit instruction "Read this file first — do not redo the investigation, continue from where it stopped", (c) update the self-monitoring block in prompts/system.md so the agent's first action checks for prior attempts of the current round number (glob `conversation_loop_{round}_attempt_*.md`) and reads them before the N-1 / N-2 prior-round check. See SPEC.md "Retry continuity" paragraph in the "Subprocess monitoring & debug retries" section
- [x] [functional] Add tests for retry continuity: verify that a crashing round writes `conversation_loop_N_attempt_1.md` and the debug retry writes `conversation_loop_N_attempt_2.md` without overwriting the first, verify the retry prompt contains the full path to the prior attempt log and the documented instruction string, verify `conversation_loop_N.md` is produced (symlink or copy) after a successful attempt for backward compatibility, and verify the agent-side self-monitoring reads prior attempts of the current round before looking at rounds N-1 / N-2
- [x] [functional] [needs-package] Add `cairosvg` as optional dependency under a `[vision]` extra in pyproject.toml, import it lazily in tui.py only when `capture_frames = true`, and log a one-line warning at startup (never blocking the run) when `capture_frames = true` but the extra is not installed — see SPEC.md "Frame capture" Dependencies paragraph
- [x] [functional] Add a test that verifies `RichTUI(capture_frames=True)` logs the documented startup warning when cairosvg is not importable, and does NOT log when capture_frames=False or when cairosvg IS importable — covers the startup-time availability check added in pyproject.toml's `[vision]` extra work
- [x] [functional] Add `capture_frame(label: str) -> Path | None` to `TUIProtocol` in tui.py: `RichTUI` uses `Console(record=True)` + `console.save_svg()` + cairosvg SVG→PNG conversion to write `runs/<session>/frames/<label>.png` and return the path; `PlainTUI` and `JsonTUI` return `None` — see SPEC.md "Frame capture" section
- [x] [functional] Wire `capture_frame` into loop.py's `_run_rounds` and convergence path: call it with labels `round_N_end`, `converged`, and `error_round_N` at the matching points, respecting the `capture_frames` config flag (off by default, on via evolve.toml / `--capture-frames` / `EVOLVE_CAPTURE_FRAMES=1`)
- [x] [functional] Extend `_run_party_mode` in loop.py to scan `runs/<session>/frames/*.png`, pick the last 3-5 frames (convergence + preceding rounds), and attach them as image blocks to each agent's prompt via the Claude Agent SDK multimodal `content` array — see SPEC.md "Phase 5 — Party mode" Inputs section
- [x] [functional] Embed captured frames inline in `evolution_report.md` under a "Visual timeline" section when `capture_frames = true`, using standard Markdown image syntax with relative paths into `frames/` — see SPEC.md "Frame capture" Bonus paragraph
- [x] [functional] Add tests for frame capture: verify `capture_frame` returns a valid PNG path on `RichTUI` (mocking cairosvg), returns `None` on `PlainTUI` / `JsonTUI`, is a no-op when `capture_frames = false`, and logs the documented warning when cairosvg is missing; verify `_run_party_mode` correctly attaches up to 5 most recent PNGs as image blocks to the agent prompt
- [x] [functional] Rewrite the `## Memory — learn from past errors` block in prompts/system.md to broaden the log trigger beyond errors: instruct the agent to append entries for errors, non-obvious decisions, surprises, recurring patterns, and architectural insights; require the structured sections (`## Errors`, `## Decisions`, `## Patterns`, `## Insights`) from the SPEC.md "memory.md" section; and change compaction from per-turn to "only when memory.md exceeds ~500 lines", with archival (not deletion) of entries older than 20 rounds under a `## Archive` section
- [x] [functional] Add an orchestrator-side byte-size sanity gate in loop.py that refuses commits where memory.md shrunk by more than 50% vs its pre-round state unless the COMMIT_MSG contains the literal string `memory: compaction` — treat violations the same as zero-progress triggers (debug retry with diagnostic header "CRITICAL — Previous round silently wiped memory.md") to catch agents that over-compact
- [x] [functional] Extract the `"memory: compaction"` sentinel and the `0.5` shrink threshold in loop.py's memory-wipe sanity gate into module-level constants (`_MEMORY_COMPACTION_MARKER = "memory: compaction"` and `_MEMORY_WIPE_THRESHOLD = 0.5`) so the documented contract in SPEC.md § "Byte-size sanity gate" cannot silently drift from the runtime check. Same discipline being planned for `_README_DRIFT_WARN_FMT`, `_PREV_ATTEMPT_LOG_FMT`, and `_CAIROSVG_MISSING_WARN`. Update tests/test_loop_coverage.py's new memory-wipe tests to import and reference those constants instead of duplicating the magic string, so any future threshold change is a single-edit propagation with a single failing test to catch drift
- [x] [functional] Update the initial memory.md template in the project (currently just a 4-line header) to pre-seed the four sections (`## Errors`, `## Decisions`, `## Patterns`, `## Insights`) so new projects start with the expected structure and agents have a concrete shape to append into
- [x] [functional] [P1] Implement the orchestrator-side enforcement of Backlog discipline Rule 1 "Empty-queue gate" from SPEC.md § "Backlog discipline": in loop.py, after every round's commit, diff improvements.md against its pre-round state; if the diff adds any new `- [ ]` line while any other `- [ ]` line exists in the post-round file, reject the commit (revert and restage without the new item, OR fail the round and trigger a debug retry with the header `"Backlog discipline violation: new item added while queue non-empty"`). Do NOT check when the queue is genuinely empty (that's the one legitimate add case). See SPEC.md § "Backlog discipline" Rule 1 for the exact contract
- [x] [functional] [P1] Expose `backlog.pending / done / blocked / added_this_round / growth_rate_last_5_rounds` in state.json per SPEC.md § "Growth monitoring": compute the fields in loop.py's `_write_state_json` by diffing improvements.md against the N-5 historical commits. Add a schema test in tests/test_loop_coverage.py that verifies the field names and types match the documented shape. Rationale: without this, rule 1 violations and queue-growth trends stay invisible; the field provides the minimum observability needed to detect discipline regressions
- [x] [functional] [P2] Add tests for Backlog discipline enforcement (rules 1-4): verify rule 1 rejects commits that add items while queue non-empty with the documented header, verify rule 2 merge behavior (extending description rather than adding), verify rule 3 priority-aware insertion order with mixed P1/P2/P3 items, verify rule 4 blocks a 4th consecutive P3 addition. Use fixture conversation logs for rule 4 input
- [x] [functional] Add tests for the memory discipline changes: verify the system prompt enforces append-only behavior for the first 500 lines, verify the 50%-shrink sanity gate triggers a debug retry with the correct header when memory.md collapses, verify the archival path moves old entries to `## Archive` instead of deleting them, and verify the new initial template is written when runs/memory.md does not yet exist
- [x] [functional] Add a SPEC-vs-prompt drift test that asserts `prompts/system.md` stays aligned with SPEC.md § "memory.md" on three canonical invariants: (a) the compaction threshold number (`500`) appears in both files, (b) the `memory: compaction` commit-message marker string appears verbatim in both, and (c) the archival-not-deletion directive and 20-round cutoff appear in both. Failure mode this catches: SPEC edit raises threshold to e.g. 1000 or changes marker to `memory: compact`, but `prompts/system.md` still teaches the old contract — agents follow stale guidance and the orchestrator's sanity gate contradicts the prompt. Single test file `tests/test_spec_prompt_sync.py` with three focused asserts; grep-level heuristic, not NLP. See SPEC.md § "memory.md" and § "Orchestrator byte-size sanity gate"
- [x] [performance] Increase loop.py test coverage from 97% to 99%+ by adding tests for the remaining uncovered lines (71-72, 541, 575-576, 584-585, 774, 791, 815, 871-872, 902, 941-942, 974-975, 1254, 1340) — these are mostly edge-case branches in subprocess monitoring, check command execution, and forever mode restart logic
- [x] [functional] Implement per-attempt conversation log files for retry continuity (part 1 of 2): in loop.py's `_run_monitored_subprocess` / `_run_rounds`, write each debug-retry attempt of round N to a distinct file `conversation_loop_N_attempt_{K}.md` (K starts at 1 for the first attempt), without overwriting prior attempts. After a successful attempt, copy (not symlink, for cross-filesystem safety) the winning attempt's file to `conversation_loop_N.md` for backward compatibility with report generation, party mode, and self-monitoring. Keep the existing `--run-dir` / log-filename plumbing intact and thread the attempt number from `_run_rounds`'s retry loop to `_run_monitored_subprocess`. Do NOT touch the prompt builder or agent-side self-monitoring yet — those are part 2 to keep this change reviewable. See SPEC.md § "Retry continuity" rule (1) [subsumed by the retry continuity (a)+(b)+(c) item above — implemented in agent.py via `_detect_current_attempt` + attempt-log plumbing in `analyze_and_fix`]
- [x] [functional] Extract cross-file-quoted SPEC strings into module-level constants with one unified drift-catching test (bundled per new backlog anti-variante rule). This is the tail of the discipline already applied in the completed `_MEMORY_COMPACTION_MARKER` / `_MEMORY_WIPE_THRESHOLD` item — do all five together in one commit with one test file `tests/test_constant_drift.py` that asserts each constant is referenced at its call-sites and the call-sites no longer hold duplicate literals. Constants to extract: (a) `_README_DRIFT_WARN_FMT` in loop.py for the Mechanism C warning (format `"README drift: {spec} touched {n} rounds ago, README.md unchanged"`) — SPEC.md § "Mechanism C"; (b) `_PREV_ATTEMPT_LOG_FMT` in agent.py for the `## Previous attempt log` section wording — SPEC.md § "Retry continuity" rule (2); (c) `_CAIROSVG_MISSING_WARN` in tui.py for the cairosvg-missing warning currently duplicated at `RichTUI.__init__` (lines 142-146) and `RichTUI.capture_frame` (lines 409-413); (d) `_MEMORY_WIPED_HEADER_FMT` in agent.py for the `"CRITICAL — Previous round silently wiped memory.md"` diagnostic-branch header — SPEC.md § "Byte-size sanity gate"; (e) `_DEFAULT_MEMORY_MD` template in evolve.py rewritten to be spec-filename-agnostic (e.g. replace `"See SPEC.md § memory.md"` with `"See your project's spec file § memory.md"`, OR plumb `spec` into `_init_config` and substitute at scaffold time). Same commit also fixes the runtime memory-section header in agent.py line 223 (`## Memory (errors from previous rounds — do NOT repeat these)` → `## Memory (cumulative learning log — read, then append during your turn)`) to align with the broadened memory.md discipline
- [x] [P3] [performance] Plumb `spec` into `_init_config` in evolve.py so the pre-seeded `runs/memory.md` can reference the project's actual spec filename at scaffold time (currently uses the spec-filename-agnostic fallback "your project's spec file §"). Thread `args.spec` (resolved via `_resolve_config`) down into `_init_config(project_dir, spec)` and substitute `{spec}` into `_DEFAULT_MEMORY_MD` at write time. Keep the spec-agnostic default when `spec` is `None` / unspecified. Update `tests/test_evolve.py` scaffold tests to cover both paths (default vs `--spec SPEC.md` scaffold). Minor polish — the current fallback is already correct and SPEC-allowed, this just makes the scaffold self-documenting in projects that use a dedicated spec file

- [x] [functional] [P1] Fix Mechanism A vs convergence gate contradiction per SPEC.md § "Mechanism A — Pre-convergence README audit" (updated): (a) in loop.py, remove the "does not block convergence" bypass — Mechanism A items are now regular `[ ]` blockers that the convergence gate enforces normally (no special-case exemption); (b) add idempotency in the audit: before appending `README sync: mention X` item, grep existing pending items for the same `<claim>` substring and skip if already present (prevents re-queueing the same gap every round); (c) add support for `[wontfix-sync: <reason>]` suffix — treated as checked/resolved for the convergence gate, and future audits must skip phrases with that suffix already present anywhere in improvements.md (including under `[x]` items). Tests in tests/test_readme_audit.py must verify: audit runs without adding duplicates on second invocation, convergence is blocked until README sync items are all `[x]` or `[wontfix-sync]`, and that a `[wontfix-sync]` phrase is never re-proposed by a later audit run
- [x] [functional] [P1] Add a convergence-gate orchestrator backstop: in loop.py, after the agent writes CONVERGED but before firing on_converged + party mode, re-verify the two documented gates (SPEC.md § "Convergence") independently of the agent's judgment: (1) `mtime(improvements.md) >= mtime(spec_file)` AND no `[stale: spec changed]` items present, (2) every `- [ ]` line in improvements.md has `[needs-package]`, `[blocked:`, or `[wontfix-sync:` tags (otherwise it's an unresolved blocker). If either fails, delete the CONVERGED file, emit `ui.error` with the violated gate, and trigger a debug retry with the header `"CRITICAL — Premature CONVERGED: <reason>"`. This closes the trust gap where Phase 4 criteria are 100% agent-judged today. See tests/test_convergence_gate.py (new file) for coverage: exhaustive gate combinations (queue empty vs non-empty, stale items present vs absent, all checked vs some unchecked with/without tags), assertion that the CONVERGED file is deleted on gate failure, assertion that the retry diagnostic carries the correct violated-gate reason
- [x] [functional] README sync: mention `EVOLVE_MODEL` in Configuration (documented in SPEC.md § Config resolution but absent from README.md)
- [x] [functional] README sync: mention `EVOLVE_SPEC` in Configuration (documented in SPEC.md § Config resolution but absent from README.md)
- [x] [functional] README sync: mention `EVOLVE_ALLOW_INSTALLS` in Configuration (documented in SPEC.md § Config resolution but absent from README.md)
- [x] [functional] README sync: mention `EVOLVE_SESSION` in Configuration (documented in SPEC.md § Hook execution but absent from README.md)
- [x] [functional] README sync: mention `EVOLVE_ROUND` in Configuration (documented in SPEC.md § Hook execution but absent from README.md)
- [x] [functional] README sync: mention `EVOLVE_STATUS` in Configuration (documented in SPEC.md § Hook execution but absent from README.md)
- [x] [functional] README sync: mention `evolve start ~/projects/my-tool --check "pytest" --forever --allow-installs` in Examples (documented in SPEC.md § The --forever flag but absent from README.md)
- [x] [functional] README sync: mention `EVOLVE_CAPTURE_FRAMES` in Configuration (documented in SPEC.md § Frame capture (visual context for party mode) but absent from README.md)
- [x] [functional] README sync: mention `evolve start . --check "pytest" --rounds 20` in Examples (documented in SPEC.md § Exit codes but absent from README.md)
- [x] [functional] README sync: mention `evolve start . --check "pytest" --rounds 20 --json > evolve-output.jsonl` in Examples (documented in SPEC.md § Exit codes but absent from README.md) [obsolete: Mechanism A removed per SPEC rewrite — README is now user-level summary, not a spec mirror; marked done to clear the queue without executing]
- [x] [functional] [P1] Revert Mechanism A (pre-convergence README audit) per SPEC.md § "README as a user-level summary": remove the grep-based claim extraction + `README sync: mention X` item queueing from loop.py and agent.py; delete tests/test_readme_audit.py and tests/test_readme_sync_discipline.py (keep only the tests that still make sense for the remaining advisory behavior); drop any mention of "Mechanism A", "README sync discipline", "wontfix-sync" from the system prompt and codebase. Do NOT revert the README content that was already added (environment variable mentions etc. — that content is legitimately useful user-level information). Only revert the automation mechanism
- [x] [functional] [P1] Revert Mechanism B (party mode README proposal) per SPEC.md § "Phase 5 — Party mode (post-convergence)": remove the `README_proposal.md` production from `_run_party_mode` in loop.py — party mode produces only `party_report.md` + `<spec>_proposal.md`. Remove the atomic dual-adoption in `_forever_restart` — only the spec proposal is adopted in `--forever` mode. Update tests/test_readme_sync_discipline.py (or delete if all assertions were Mechanism-B-specific). The README is now user-authored, never written by the evolution loop
- [x] [functional] [P1] Revert Mechanism C (mtime drift warning inside the round loop) per SPEC.md § "Stale-README pre-flight check": remove the per-round mtime comparison and `readme_sync` field from state.json (drop the field entirely from `_write_state_json`), remove the `ui.warn` call from `_run_rounds`, delete tests/test_readme_drift.py and tests/test_readme_drift_integration.py. Replace with a much simpler startup-time advisory described below
- [ ] [functional] [P1] Implement the startup-time stale-README advisory per SPEC.md § "Stale-README pre-flight check": at the very beginning of `evolve start` (in loop.py or evolve.py, before the first round), when `--spec` points at a file other than `README.md`, compare `mtime(spec_file) - mtime(README.md)`; if > configured threshold (default 30 days, read from `evolve.toml [tool.evolve] readme_stale_threshold_days`, 0 disables entirely), emit a single-line blue `ui.info` advisory `"ℹ️  README has not been updated in N days — consider `evolve sync-readme`"`. No state.json field, no blocking, no per-round tracking. Add tests verifying the advisory fires at threshold boundaries, is silent below threshold, is silent when --spec is unset, is silent when threshold is 0
- [ ] [functional] [P1] Implement the `evolve sync-readme` subcommand per SPEC.md § "evolve sync-readme": new CLI entry `evolve sync-readme [project-dir] [--spec SPEC.md] [--apply]` in evolve.py. Loads the spec file (same resolution order as other flags) and current README, launches agent in a one-shot session with the documented sync-focused prompt, writes `README_proposal.md` (default) or applies directly to README.md + git commit (`--apply`). Exit codes 0 (proposal written / applied), 1 (README already in sync), 2 (error). Tests must cover: proposal mode writes file without modifying README, apply mode writes README and creates commit, exit-1 path when agent reports no changes needed, exit-2 path when spec missing, correct behavior with --spec flag, correct behavior when no --spec is set (README IS the spec, subcommand should refuse or no-op with message)
